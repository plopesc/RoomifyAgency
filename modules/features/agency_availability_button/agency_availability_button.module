<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function agency_availability_button_ctools_plugin_directory($module, $plugin) {
  if (($module == 'ctools') && ($plugin == 'content_types')) {
    return 'plugins/content_types';
  }
}
